<%= link_to_if action_name == 'index', page.title, page %>
<%= simple_format page.body %>
<%= link_to 'Verwijderen', page_path(page), method: :delete, data:{confirm: 'Weet u het zeker?', class: 'btn btn-sm btn-danger'} %>
